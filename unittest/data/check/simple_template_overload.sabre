package main

type Vec struct<T: type> {
	x, y, z: T,
}

var vf: Vec<float> = {
	x = 1.5,
};

var vi: Vec<int> = {
	x = 1,
};

func add(a: Vec<float>): int {
	return 0;
}

func add(a: Vec<int>): int {
	return 0;
}

func foo() {
	add(vf);
	add(vi);
}