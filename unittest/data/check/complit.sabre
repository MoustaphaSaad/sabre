type Point struct {
	x, y: float
}

func add(a, b: Point): Point {
	return :Point {
		x = a.x + b.x,
		y = a.y + b.y,
		z = a.x + b.y,
	};
}

func add2(a, b: Point): Point {
	return :Point {
		x.sdwert = a.x + b.x,
		y = a.y + b.y,
	};
}
