package main

const dims = 4;

if dims == 2 {
	type Point struct {
		p: vec2
	}
} else if dims == 3 {
	type Point struct {
		p: vec3
	}
} else {
	type Point struct {
		p: vec4
	}
}

func add(a, b: Point): vec3 {
	return a.p + b.p;
}