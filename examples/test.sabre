package main

import "std"

@uniform
var image: Texture2D<int>;

@uniform
var sampler: Sampler;

@compute{x = 16, y = 16}
func main(@system_thread_id index: uvec3) {
	var pixel: vec4 = std.texture_sample(image, sampler, index.xy: vec2);
}