package main

type Vec struct<T, R: type> {
	x, y, z: T,
	r, g, b: R,
}

var vf: Vec<float, float> = {
	x = 1.5,
	r = 2.5,
};

var vi: Vec<int, float> = {
	x = 1,
	r = 2.5,
};

func add(a: Vec<float>): int {
	return 0;
}

func add(a: Vec<int>): int {
	return 0;
}

func foo() {
	add(vf);
	add(vi);
}